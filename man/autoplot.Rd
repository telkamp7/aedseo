% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R
\name{autoplot}
\alias{autoplot}
\alias{autoplot.tsd}
\alias{autoplot.tsd_onset}
\title{Create a complete 'ggplot' appropriate to a particular data type}
\usage{
autoplot(object, ...)

\method{autoplot}{tsd}(object, time_interval = "5 weeks", ...)

\method{autoplot}{tsd_onset}(
  object,
  linewidth = 0.7,
  size = 2,
  alpha = 0.3,
  width = 0.2,
  time_interval = "5 weeks",
  ...
)
}
\arguments{
\item{object}{An \code{tsd} or \code{tsd_onset} object}

\item{...}{Additional arguments (not used).}

\item{time_interval}{A character vector specifying the time interval and how
many time steps desired on the x-axis, e.g. "10 days," "4 weeks," or "3 months."}

\item{linewidth}{Numeric, the width of the line for the growth rate}

\item{size}{Numeric, size of observational points.}

\item{alpha}{Numeric, the alpha (transparency) for the observations with a
significantly positive growth rate.}

\item{width}{Numeric, the width of the error bar employed to show the
confidence interval of the growth rate estimate.}
}
\value{
A 'ggplot' object for visualizing the time series data.
}
\description{
This function generates a complete 'ggplot' object suitable for visualizing time series data in an \code{tsd} object.
It creates a line plot connecting the observations and adds points at each data point.
}
\examples{
# Create an example `tsd` object
time_series <- tsd(
  observation = c(100, 120, 150, 180, 220, 270),
  time = as.Date(c(
    "2023-01-01",
    "2023-01-02",
    "2023-01-03",
    "2023-01-04",
    "2023-01-05",
    "2023-01-06"
  )),
  time_interval = "day"
)

# Create a ggplot visualization for the `tsd` object
autoplot(time_series)

# Create an `tsd_onset` object
time_series_with_onset <- seasonal_onset(
  tsd = time_series,
  k = 3,
  level = 0.95,
  family = "quasipoisson"
)

# Create a ggplot visualization of growth rates with confidence intervals
autoplot(time_series_with_onset, linewidth = 1, alpha = 0.2)
}
